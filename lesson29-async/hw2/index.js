// in: userId-string, callback - function;
// out:undefined

// log
// 1. Make getRandomArbitrary
// 2.

const requestUserData = (userId, callback) => {};
// --------------------------------
function getRandomArbitrary() {
  return Math.round(Math.random() * (3 - 1) + 1);
}
console.log(getRandomArbitrary());
